description = "Atomix integration component"

dependencies {
	compile project(':ar4k-core')
	compile group: 'com.hazelcast', name: 'hazelcast', version: '3.12.11'
	compile group: 'com.hazelcast', name: 'hazelcast-kubernetes', version: '2.2.1'
}

bootJar {
	enabled = false
}

